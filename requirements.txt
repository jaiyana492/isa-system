# ─────────────────────────────────────────────────────────────────────────────
# CorePilora AI ISA — Requirements
# Every dependency traced from actual imports across all project files.
# ─────────────────────────────────────────────────────────────────────────────

# ── Framework ─────────────────────────────────────────────────────────────
# main.py, api/v1/*.py, services/validator.py
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12

# ── Configuration ─────────────────────────────────────────────────────────
# config/settings.py
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1

# ── Database ──────────────────────────────────────────────────────────────
# config/database.py, models/*.py, services/conversation.py,
# services/scheduler.py, services/nurture.py, services/communication.py
sqlalchemy[asyncio]==2.0.35
asyncpg==0.31.0

# ── Redis ─────────────────────────────────────────────────────────────────
# config/redis.py, services/cache.py, services/audio_cache.py
redis[hiredis]==5.1.0

# ── HTTP Client ───────────────────────────────────────────────────────────
# agents/buyer_graph.py, seller_graph.py, investor_graph.py (Groq calls)
# services/communication.py (Deepgram, ElevenLabs)
# services/crm.py (HubSpot)
# api/v1/health.py (Groq health check)
httpx==0.27.0

# ── Twilio ────────────────────────────────────────────────────────────────
# services/communication.py
twilio==9.3.0

# ── Phone Validation ──────────────────────────────────────────────────────
# models/lead.py
phonenumbers==8.13.47

# ── LangGraph ─────────────────────────────────────────────────────────────
# agents/buyer_graph.py, seller_graph.py, investor_graph.py, graph.py
langgraph==0.2.76

# ── WebSockets ────────────────────────────────────────────────────────────
# api/v1/voice.py (Deepgram STT WebSocket client)
websockets==12.0